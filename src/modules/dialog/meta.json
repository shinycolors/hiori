{
  "name": "dialog",
  "description": "Translates dialog directly inside the game",
  "options": [
    {
      "id": "enabled",
      "title": "Enabled",
      "desc": "Toggle this feature",
      "type": "check",
      "default": true
    }
  ]
}
