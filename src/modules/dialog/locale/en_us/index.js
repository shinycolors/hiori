import names from './names.json'
import produce from './produce.json'
import hioriEnd from './hiori-end.json'

export default []
  .concat(names.messages)
  .concat(produce.messages)
  .concat(hioriEnd.messages)
